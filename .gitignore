.DS_Store
node_modules
/dist

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
src/compiled-icons

# CLAUDE.md is ignored on every nested path (private per-checkout notes),
# except the tracked public root file re-included below. .claude/ is the
# maintainer's private ops checkout (separate repo), not part of the product.
CLAUDE.md
!/CLAUDE.md
.claude/

# claude-container runtime settings (may contain host-specific paths)
.claude-container.d/env
