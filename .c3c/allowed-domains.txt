# Additional domains for sotlas-frontend package installs.
# FontAwesome Pro is replaced by local stubs and aliases, so npm.fontawesome.com is not needed.
# Playwright MCP uses the lockfile-pinned local install in .c3c/playwright-mcp/;
# it does not download packages from npm when a session starts.
registry.npmjs.org
# package.json の github: 依存（vue-keycloak-js, mapbox-gl-draw, frappe-charts,
# vue-mapbox, vue-native-websocket）の tarball 取得に必要。
codeload.github.com
# Retained GitHub Copilot API allowance; project Issue operations now use gh.
api.githubcopilot.com
# Codex CLI authentication and API access (https://chatgpt.com/backend-api/).
chatgpt.com
auth.openai.com
# Context7 MCP サーバー（ユーザースコープ ~/.claude.json の mcpServers、type: http）。
# ライブラリの最新ドキュメント参照用（内部運用issue参照）。
mcp.context7.com

# --- ここから Playwright MCP（.mcp.json）によるコンテナ内 WebUI 検証用 ---
# アプリ実行時に叩く外部ドメイン。ワイルドカード非対応のため個別列挙。
# Turnstile スクリプト（App.vue が未認証時に常時描画）。トークン→/mapkey/get→
# MapTiler キー取得の連鎖の起点で、これが無いと地図が一切描画されない。
challenges.cloudflare.com
# 地図スタイル/タイル/グリフ/スプライト（src/mixins/mapstyle.js にハードコード）。
api.maptiler.com
# API ベース（VITE_API_URL）・/tracks・/sprites。
sotl.as
# SOTA DB API。
api-db2.sota.org.uk
api-db.sota.org.uk
# 写真サムネイル（VITE_PHOTOS_URL）。
photos.sotl.as
# 写真オリジナル（VITE_PHOTOS_ORIGINAL_URL）。
sotlas-photos.s3.eu-central-003.backblazeb2.com
# 標高 API（summit 詳細ページで発火）。
elevation.sotl.as
# 標高 API の切替先（upstream c3acf47、2026-09-18。.env の VITE_ELEVATION_API_URL 参照）。
elevation.racemap.com
# アクティベーションゾーン GeoJSON。
az.sotl.as
# 検索機能（SearchField.vue）。
secure.geonames.org
# ウェブカム一覧 API（既定 OFF の機能だが先回りで許可）。
api.windy.com
# Wikipedia ウィジェット（summit 詳細ページの関連記事取得用と推測）。未収載だと
# ERR_ADDRESS_UNREACHABLE で「Network or server error」トーストが出る（検証中に発見）。
en.wikipedia.org
# 注: sso.sota.org.uk は追加しない（Keycloak init は wantSso フラグ時のみで、
# SSO 検証は今回の対象外）。
# 注: live spot の WebSocket は Vue 3 版では `src/websocket.js` の自前実装（vue-native-websocket は
# 廃止）。接続先は .env の VITE_WSS_URL（wss://sotl.as/api、上の sotl.as で許可済み）。
# 注: 代替ベースマップ（swisstopo 等）・Windy サムネイル画像ホストは、必要になった
# 検証時に browser_network_requests で実ホストを確認してから追記する。
