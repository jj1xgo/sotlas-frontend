# sotlas-frontend 用の追加 apt パッケージ。
# Node.js は node-version.txt（nodejs.org 公式 tarball、/usr/local/bin へ展開）で
# package.json の engines と一致するバージョンを導入しており、PATH 優先順位
# （/usr/local/bin が /usr/bin より先）により apt 版より常に優先される。
# そのため nodejs/npm を apt でも入れる意味がなく、ここには置かない。
