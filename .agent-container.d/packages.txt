# sotlas-frontend 用の追加 apt パッケージ。
# Node.js は node-version.txt（agent-container のproject image）で package.json の
# engines と一致するバージョンを導入しており、PATH 優先順位
# （/opt/project-node/bin が共通Nodeより先）により常に優先される。
# そのため nodejs/npm を apt でも入れる意味がなく、ここには置かない。

# Playwright MCP（.mcp.json）のヘッドレス検証用ブラウザ。実行時aptは不可なため
# ビルド時に導入する。apt の依存解決で CDP/レンダリングに必要な共有ライブラリ一式
# （libnss3, libatk, libgbm 等）が自動的に揃う。
chromium
# 上記ブラウザでのスクリーンショットが豆腐化しないためのフォント。
fonts-liberation
fonts-noto-core
fonts-noto-cjk
